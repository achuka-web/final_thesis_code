# Smoke Test Checklist

This placeholder will be replaced with a simple validation checklist once the pipeline code is implemented.

Planned checks:
- Raw files load correctly
- Required columns exist
- Log returns are computed
- Statistical diagnostics run end-to-end
- Pattern tests produce outputs for `h=1,3,5`
- Backtest and transaction-cost summaries run without notebook state
